# Dopabae Implementation Plan

> **この文書の役割**：本リポジトリの**実装順序**（Phase 1〜6）。
> **本書が実装順序の正である**（[`../CLAUDE.md`](../CLAUDE.md) の文書役割表に対応）。
> 予定は進行に応じて変わるが、変更は本書を更新して行う。
> 実装してよい範囲は [`REPOSITORY_PLAN.md`](REPOSITORY_PLAN.md)、
> 必ず守るルールは [`../CLAUDE.md`](../CLAUDE.md)、
> 設計の背景は [`DESIGN_MEMO.md`](DESIGN_MEMO.md) を参照する。
> 本書の Phase は、設計メモ 5 章「実装順序（案）」を段階に分けたものである。

## 1. このリポジトリの目的

このリポジトリでは、AIエージェントファームの2体目の試験エージェント
「ドパバエ」（Dopabae）を開発する。

ショウジョウバエのコネクトームを LIF ニューロンで動かし、15分足のチャート画像を
「目」に見せて、その反応を売買の**方向**（近づく／逃げる／どちらでもない）に使う。
数量・価格・上限・諦めは決定的コード（檻）が担い、ペーパートレードで運用する。
**実取引は行わない。現物のみを扱う。**

儲けることが目的ではない。目的は次の2つ（設計メモ 1 章）。

1. 重み固定のハエの反応が、ランダムと区別できる偏りを持つか観察する
2. 報酬学習（ドーパミンによる可塑性）を足したとき、挙動がどう変わるか観察する

## 2. 現在できているもの

| #   | 対象                          | 状態                                                   |
| --- | ----------------------------- | ------------------------------------------------------ |
| 1   | `CLAUDE.md`                   | 不変のルール（ナンピノニクスから引き継ぎ、ハエ脳の制約を追加） |
| 2   | `docs/REPOSITORY_PLAN.md`     | 担当範囲                                               |
| 3   | `docs/DESIGN_MEMO.md`         | 設計メモ（ハエが方向を決め、コードは檻。現物のみ）     |
| 4   | `docs/IMPLEMENTATION_PLAN.md` | 本書                                                   |
| 5   | `agent.yaml`                  | 型だけ。名前は確定、檻の値はナンピノニクスの値、ハエ脳の値は候補値 |
| 6   | `.claude/settings.json`       | 禁止コマンド（実取引・paper reset）のハーネス側での二重化 |
| 7   | `personality.md`              | 性格と話し方。パラメータは実装の数値から導き、偏りは Phase 4 で測るまで空欄 |
| 8   | `tests/` / `.github/workflows/` | `agent.yaml` の不変ルール検証と、テスト・セキュリティ点検の CI（ナンピノニクスと同じ構成） |
| 9   | `dopabae/`                    | エージェント本体（Phase 1）。観測 → 方向 → 檻 → 発注 → 記録が1周する。`python3 -m dopabae.run` |
| 10  | `strategy.md` / `risk-policy.md` / `memory-policy.md` | 檻・リスク制約・記憶方式の説明。値の正は `agent.yaml` |
| 11  | `status.yaml`                 | 状態のスキーマの見本。実行時は `var/status.yaml` に書く |
| 12  | `dopabae/vision.py`           | ハエに見せる画像（Phase 2 前半）。15分足から 320×180 を決定的に描き、ハッシュと足の範囲を判断ログに残す |
| 16  | `dopabae/retina.py` / `scripts/check_column_map.py` | 画像から光受容細胞への入力（Phase 2 後半）。R1–R6 に輝度、R8 に青／緑の代理値を渡す。座標表は Git 管理外 |
| 13  | `dopabae/performance.py` / `evaluate.py` / `scripts/make_arm.py` | 評価基盤（Phase 4 の土台）。腕を並べて比べる |
| 14  | `dopabae/lock.py` / `scripts/run_arms.py` / `scripts/launchd/` / `scripts/systemd/` | 定期実行。腕を並行して回し、同じ腕の二重起動を防ぐ |
| 15  | `dopabae/summary.py` / `narrate.py`                            | 日次サマリと記録の言語化。判断には関与しない |

檻（Phase 1）、画像の描画とマッピング（Phase 2）、評価基盤（Phase 4 の土台）、
定期実行、記録の言語化は実装済み。
**シミュレーション（Phase 3）はまだ存在しない。**
評価そのものは、比べる相手（ハエ版）が出てから回す。

#### `dopabae/` のモジュール構成

| モジュール     | 責務                                                                 | 由来 |
| -------------- | -------------------------------------------------------------------- | ---- |
| `config.py`    | `agent.yaml` の読み込みと検証。数値パラメータの入口はここだけ        | 書き直し |
| `cli.py`       | `bitbank` コマンドの薄いラッパ。禁止コマンドは組み立てた時点で例外   | ナンピノニクスからコピー |
| `observe.py`   | 取引所のガードと気配（買い気配・売り気配）の観測                     | 書き直し |
| `state.py`     | `bitbank paper` の実測から状態を導出。階段を外し `Activity` に       | 書き直し |
| `orders.py`    | 数量・価格の丸め、発注・取消                                          | ナンピノニクスからコピー |
| `direction.py` | 方向の出どころ（`always_approach` / `random`。`fly` は Phase 3）      | 新規 |
| `cage.py`      | 檻。方向を受け取り、数量・価格・上限・諦めを決める純関数             | 新規（`decide.py` の代替） |
| `journal.py`   | 判断ログ。ハエの答え（`direction`）と檻の扱い（`cage`）を分けて残す  | 書き直し |
| `run.py`       | 1周のオーケストレーション。唯一のエントリポイント                    | 書き直し |
| `timeutil.py`  | 時刻の扱い                                                            | ナンピノニクスからコピー |
| `vision.py`    | ハエに見せる画像。自前のバッファと 5×7 の書体で描き、PNG は zlib だけで書く | 新規 |
| `retina.py`    | 画像から光受容細胞への入力。座標表を読み、範囲の平均から輝度と青／緑を採る | 新規 |
| `performance.py` | 成績の物差し。総資産は判断ログの**観測値**から取り、復元しない        | 書き直し |
| `evaluate.py`  | 腕を並べて比べる。方向の頻度と偏り、檻の扱い、方向のあとの値動き、成績 | 新規 |
| `lock.py`      | 同じ腕の二重起動を防ぐ（`flock`）。重なった回は何もしない            | 新規 |
| `summary.py`   | 日次サマリ。ハエの言葉の欄は空欄で作り、数字は別の節へ転記する      | 書き直し |
| `narrate.py`   | 空欄をドパバエの言葉で埋める。**売買の判断には関与しない**          | 書き直し |

## 3. 着手前に確認すること（未確認の前提）

以下は**まだ確認できていない**。満たせると判断して進めない。
確認できたら本節を更新し、確認した日付と結果を書く。

| #   | 前提                                        | 状態     | 影響する Phase |
| --- | ------------------------------------------- | -------- | -------------- |
| 1   | Stonkfly の環境要件：Python 3.11 / C++17 / メモリ 16 GB | **2026-09-15 調査。実測の根拠は見つからなかった**（[`CONNECTOME_SURVEY.md`](CONNECTOME_SURVEY.md) 4 章）。要件の数字は README の記載のみ | 2〜6 |
| 2   | 全脳・1観測（神経時間 0.5 秒）の計算時間     | **2026-09-15 調査。Stonkfly 自身の実測値は無い。** 他実装からの外挿しかない。自分で測る以外にない | 3〜6 |
| 3   | 配線図のデータソース（MaleCNS v1.0 か FlyWire か） | **2026-09-16 決定：MaleCNS v1.0**（[`CONNECTOME_SURVEY.md`](CONNECTOME_SURVEY.md) 6.3）。`agent.yaml` の `fly.connectome.source` に置いた | 2〜6 |
| 4   | 配線図のライセンス（再配布・派生物の扱い）  | **2026-09-16 に人間が一次資料で確認。結果はデータソースによって分かれる**（[`CONNECTOME_SURVEY.md`](CONNECTOME_SURVEY.md) 3 章）。MaleCNS は CC BY 4.0 で確定し、着手を止めるものは残っていない。FlyWire は NC の有無が食い違ったまま確定せず、**選ぶなら Princeton への照会が先**（同 3.3） | 2〜6 |
| 5   | 接近側・回避側の読み出しに使う細胞群が、MaleCNS v1.0 に注釈付きで存在するか | 未確認。型名の存在は調査で報告されたが未検証で、個別の同定の頑健さも未確認（同 2 章）。**データを取得した時点で自分で確かめる** | 3〜6 |
| 6   | シナプス数の閾値（6.2M / 10.5M / 25.6M と桁が動く） | **未決。モデルの選択であってデータの性質ではない**（同 2.1 章） | 3〜6 |
| 7   | 定期実行の環境で 1 を満たせるか（15分ごとに回す計算資源） | 未確認 | 3〜6 |
| 8   | ナンピノニクスの `cli` / `orders` が今の `bitbank-lab-cli` でそのまま動くか（版差） | **2026-09-15 に確認済み。** `bitbank-lab-cli` 0.5.0、Python 3.14、macOS で 1 周が通った（下記「状態」） | 1 |

- 1 は Stonkfly の README に書かれた値をそのまま写したもので、実測の根拠は
  調査でも見つからなかった。本リポジトリの環境で満たせるかは別問題である。
- 3・4 は片づいた。残る未決（シナプス数の閾値、切り出しの範囲、読み出しに使う
  ニューロン群、静止発火率の扱い）は [`CONNECTOME_SURVEY.md`](CONNECTOME_SURVEY.md)
  6 章に並べてある。判断は人間が行い、値は `agent.yaml` に置く。
- **3 と 4 が片づいたので、Phase 2 後半に着手できる。** MaleCNS v1.0 の
  CC BY 4.0 は一次資料で確認済みで、ライセンスを理由に止めるものは残っていない。
  FlyWire は採らないため、NC の食い違い（同 3.3）は本リポジトリに影響しない。
- **Codex 由来のダウンロード URL を記録に残さない。** URL に API トークンが
  含まれる（同 3.6）。`gs://` と Zenodo の URL はトークン不要である。
- **キノコ体まわりだけを切り出す案は消えた。** 視覚入力が KC に届く経路が切れるため
  （同 2.3 章）。全 CNS か、視覚から下行性までを残す切りかたのいずれかになる。
- `bitbank paper` は現物のみ（`side` は buy / sell、売りは保有量のロックが必要、
  `paper pnl` は負の建玉をエラーにする）。2026-09-15 に CLI の実装で確認した。

## 4. 責務の分離

以下の責務を混同しない。

### ハエ脳（シミュレーション）

チャート画像を受け取り、LIF シミュレーションを回し、接近側・回避側の
ニューロン群の発火数を返す。**ここは売買を知らない。** 残高・損益・保有量は見せない。

### 読み出し（発火率 → 方向）

「接近の指標 − 回避の指標」の1本の軸を閾値で三値（`APPROACH` / `AVOID` / `NONE`）に
落とす。設計者が決めた読み替え。閾値・対象ニューロン・神経時間は `agent.yaml` に置く。
ハエが書き換えない。「ハエの脳に売買を判断する細胞がある」という意味ではない。

### 方向の出どころ（`direction.source`）

方向を返すものは差し替えられる。ハエ版のほかに、評価用の「常時 `APPROACH`」と
「ランダム」を持つ。どれも同じ契約（入力：観測、出力：方向と根拠）を満たす。
返せなかった・読めなかったときは `direction.on_failure`（既定 `hold`）に従う。

### 檻（決定的コード）

方向を受け取り、数量・価格・建玉上限・現金比率・クールダウン・保有上限・
ドローダウン停止を `agent.yaml` の値で決めて、取消と発注を組み立てる純関数。
ハエがどう答えても最悪ケースはここで決まる。I/O を持たない。

### 報酬と可塑性

決済が確定した回だけ、確定損益から報酬（PAM11）/ 嫌悪（PPL101）の刺激を決める。
変わるのは KC→MBON のシナプス重みだけ。報酬の閾値は `agent.yaml`。

### 評価

固定版・学習版・檻だけ・ランダムを同じ物差しで比べる。
これが揃うまで「学んだ」とは言わない（設計メモ 4 章）。

## 5. 開発順序

| Phase | やること                                 | 設計メモ 5 章 | 完了の目安                                                       |
| ----- | ---------------------------------------- | ------------- | ---------------------------------------------------------------- |
| 1     | 檻と方向インタフェース（**実装済み**）   | 1             | 「常時 APPROACH」と「ランダム」を方向の出どころにして paper で1周する |
| 2     | チャート描画と光受容細胞へのマッピング（**実装済み**） | 2 | 15分足から 320×180 の画像が描け、R1–R6 / R8 への入力配列に落ちる |
| 3     | 重み固定のシミュレーションで方向を返す   | 3             | ハエ版が paper で1周し、判断ログに発火数と方向の根拠が残る       |
| 4     | 評価（固定版）。**土台は実装済み**       | 4             | 評価 1・3・4 の結果が記録され、ランダムとの差と偏りが読める      |
| 5     | 可塑性と報酬                             | 5             | 確定損益からドーパミン刺激が出て、KC→MBON の重みが変わる         |
| 6     | 評価（学習版）                           | 6             | 評価 1〜4 の結果が記録される                                     |

学習を最後にするのは、「学習の効果」と「読み出しの偏り」を分けて見るためである。
Phase の順序は入れ替えない。Phase 5 を Phase 4 より先に始めない。

### Phase 1：檻と方向インタフェース

ナンピノニクスの `config` / `cli` / `observe` / `state` / `orders` / `journal` / `run` を
本リポジトリへ**コピーして**持ち込み、`decide.py` を檻に置き換える。
ナンピノニクスの `veto.py` は持ち込まない。

#### 決めたこと

| 項目             | 決定                                                                  |
| ---------------- | --------------------------------------------------------------------- |
| 持ち込み方       | コピー。共通ライブラリ化しない（`REPOSITORY_PLAN.md`「扱わないもの」2） |
| 方向の契約       | 入力：観測（画像または事実）／ 出力：`APPROACH` / `AVOID` / `NONE` と根拠 |
| 最初の出どころ   | 「常時 `APPROACH`」と「ランダム」（`agent.yaml` の `direction.source`）。ハエ版は Phase 3 |
| 失敗時           | `direction.on_failure`（既定 `hold`）。出どころの種類によらず同じ     |
| 檻の入力         | 方向、観測、現在状態。出力は「何を取り消し、何を発注し、なぜそうしたか」 |
| 買い             | `APPROACH` かつ建玉が上限未満かつクールダウン外のとき、固定額の指値を買い気配に置く |
| 売り             | `AVOID` かつ建玉ありのとき、全量を指値で売り気配に置く。時間切れ・強制手仕舞いは方向によらず檻が出す |
| 未約定の指値     | 次回までに約定しなければ取り消す。二重発注しないため、本数は `paper active-orders` から数える |
| 現物のみ         | 建玉なしの `AVOID` は何もしない。記録には `AVOID` と残す              |
| LLM              | 記録の言語化（`narrate.py`）だけ。売買には関与しない                  |

#### 完了の目安

- 方向の出どころを `direction.source` で切り替えられる
- 「常時 `APPROACH`」で、檻の上限・クールダウン・諦めがテストで担保される
  （建玉上限で止まる、現金比率を割らない、時間切れで手仕舞う、ドローダウンで新規買いが止まる）
- 判断ログの `direction` に、出どころの種類・答え・根拠・失敗理由と、
  檻がそれをどう扱ったか（`cage`）が残る

#### 状態（2026-09-15）

テスト（`tests/test_cage.py` / `tests/test_run.py`）で担保したうえ、
**実際のペーパー口座で 1 周が通った。**

| 確認したこと | 結果 |
| --- | --- |
| 環境 | macOS、`bitbank-lab-cli` 0.5.0、Python 3.14 |
| 観測 | 10 コマンドすべて成功。`error: null` / `warnings: []` |
| 画像 | 15分足 8 本（14:30〜16:15）から描画。ハッシュと PNG が残った |
| 方向 | `always_approach` が `APPROACH` を返した |
| 檻 | 買い気配 11,985,377 に 0.0083 の買い指値を組み立てた（`cage: bought`） |
| 数量 | 100,000 ÷ 11,985,377 = 0.0083435 → 刻み 0.0001 へ切り捨てて 0.0083。約 99,479 JPY で予算内 |
| 記録 | 判断ログ・`var/status.yaml`・`var/performance.yaml` が書かれた |

CLI の版差（3 節 8）はこれで潰れた。`dry_run` を外すかどうかは人間が判断する。

### Phase 2：チャート描画と光受容細胞へのマッピング

Stonkfly の `docs/model.md`「What the fly sees」を参照して、15分足から
チャート画像を描き、光受容細胞への入力に変換する。

#### 決めたこと

| 項目             | 決定                                                                      |
| ---------------- | ------------------------------------------------------------------------- |
| 画像             | 320×180 RGB、明るい背景（Stonkfly と同じ。暗い背景では KC がほぼ発火しなかったため） |
| 見せるもの       | 直近の15分足（本数は `agent.yaml`。目安は2時間〜1日）、通貨ペア、売り気配・買い気配 |
| 見せないもの     | 残高・損益・保有量                                                       |
| 入力先           | R1–R6：輝度（線形 sRGB）、R8：青／緑の代理値                              |
| マッピングの根拠 | 配線図側のカラム注釈から推定（Stonkfly の方法）。**配線図の選定（3 節 4）が終わるまで着手できない** |
| 記録             | 画像のハッシュと描画に使った足の範囲を判断ログに残す                      |

#### 完了の目安

- 同じ足を与えれば同じ画像（同じハッシュ）が出る
- 画像から入力配列（受容細胞 ID → 電流）が作れる。未マッピングの受容細胞に値を作らない
- 描画に失敗した回は入力を作らず、上位が HOLD できる

#### 状態（2026-09-15）

前半（描画と記録）を実装した。

- `dopabae/vision.py`：15分足（本数は `fly.vision.lookback_candles`）・ペア名・買い気配・売り気配だけを描く。
  残高・損益・建玉は引数にも取らない。`fly.vision.show` に見せてはならないものが入っていれば設定として拒否する
- 描画は自前のバッファと 5×7 のビットマップ書体で行い、依存を増やしていない。
  ハッシュは PNG ではなく生の RGB バイト列に対して取る
- 毎周、判断ログの `vision` にハッシュ・大きさ・足の種類と本数・足の範囲・描いたものを残し、
  PNG を `memory.vision.path`（Git 管理外）に保存する
- 描けなかった回は `vision: null` と警告を残す。対照群（`always_approach` / `random`）は画像を見ないので
  判断は続ける。ハエ版（Phase 3）では画像の欠損を方向の失敗として HOLD にする
- 色は `fly.vision.palette` に置いた。R8 は青／緑の代理値を受け取るので、後半で見直す対象

#### 後半で決めたこと（2026-09-16）

実機で 1 周したときの画像（15分足 8 本、スプレッド 1 円）を見て分かったこと。

**1. 買い気配の線が売り気配の線に隠れる**

スプレッドが 1 円だと、両方が同じ画素の行に落ちる。いまは買い気配（青）を先に、
売り気配（橙）をあとに描くので、**青が完全に消える。**

これは描画の見た目の問題ではない。**R8 は青と緑を受け取る担当**なので、
青い線が消えると、その入力がまるごと失われる。BTC/JPY のスプレッドが常にこの程度なら、
青は毎回消えることになる。

| 案 | 中身 | 難点 |
| --- | --- | --- |
| 1 | 描く順を逆にして青を勝たせる | 今度は橙が消える |
| 2 | 重なるときだけ 1 画素ずらす | **データに無い差を描く**ことになる |
| 3 | そのままにする | 青がまるごと消える |
| **4** | **重なった行は両方の色を混ぜる** | 配色に無い色が 1 行だけ現れる |

**4 を採った。** 同じ場所を 2 本が占めているなら、そこから届く光は 2 色の混合である。
これは描画の都合ではなく、画像が表しているものの素直な帰結である。青の寄与は
比例して残り、スプレッドに対して跳ばずに変わる。1 と 3 は入力を 1 つ捨てることになり、
2 はその解像度に無い差を描くことになる。

混色は `vision._mix` が採る 2 色の平均で、新しい設定値は増やしていない
（`agent.yaml` の `bid` と `ask` から決まる）。

**測った効果。** 実データに近い格子（37×37、R1–R6 が 8,214 個、R8 が 1,369 個）で、
スプレッド 1 円の画像を作って比べた。

| 確かめたこと | 結果 |
| --- | --- |
| 混色が入力に届いているか | 青を消すと **1,369 個中 37 個の R8 の値が変わる**。混色はこの入力を残している |
| 気配が入力に届いているか | 気配だけを動かすと 1,369 個中 44 個の R8 の値が変わった |

#### 後半で決めたこと（マッピング）

| 項目 | 決定 | 根拠 |
| --- | --- | --- |
| 座標の出どころ | MaleCNS v1.0 の `assignedOlHex1` / `assignedOlHex2` を人間が TSV へ書き出す | データはリポジトリに含めない（[`CONNECTOME_SURVEY.md`](CONNECTOME_SURVEY.md) 3.6） |
| 格子から画像へ | 軸座標を平面へ置き（`hex1 + hex2/2`、行間 √3/2）、広がりを画像いっぱいに引き伸ばす | **設計者が決めた対応づけ。複眼の視野角とは無関係である** |
| 1 個が見る範囲 | 格子の目の粗さから決める（実データ規模で 3×5 画素）。`fly.retina.field_px` で固定もできる | **1 画素で拾わない。** 気配の線は 1 画素の破線なので、点で拾うと当たるかどうかが運で決まる |
| R1–R6 | 範囲の平均を線形 sRGB に直し、Rec. 709 の輝度を渡す | ガンマを外してから平均する。外さないと明るい側に偏る |
| R8 | 線形の青と緑の平均（`fly.retina.r8_channel`） | 実際の R8 は個眼ごとに Rh5（青）と Rh6（緑）へ分かれるが、**その注釈が MaleCNS にあるかは未確認**。いまは 1 本の代理値にする |
| 向き | `flip_x: false` / `flip_y: true` | **複眼の視野との対応は未確認。** Phase 4 の腕にできる |
| 出す値 | 0〜1 に正規化した明るさ | LIF への電流に直す倍率は Phase 3 で実測してから `agent.yaml` に置く。ここでは決めない |
| 座標の無い細胞 | **値を作らない。数だけ残す** | `CLAUDE.md`「観測していない値を書かない」 |
| 型名が噛み合わないとき | 空の入力を返さず落とす | 空の入力は「真っ暗な世界」と区別がつかない |

**読み替えの責任はすべて設計者にある。** 実際の複眼はレンズの光学・神経重畳・
眼の曲率を通して世界を見ている。ここで行うのは「平面の画像を六角格子へ配る」
という対応づけであり、生体の視野との一致は検証されていない。
「ハエがチャートを見ている」とは書かない。

#### 座標表がまだ無い

`fly.retina.column_map_path`（既定 `var/column_map.tsv`、Git 管理外）に
人間が置く。置いたら `python3 scripts/check_column_map.py` で、
型名・座標の欠け・1 個が見る範囲を確かめる。

**型名（`R1`〜`R6` / `R8`）が MaleCNS v1.0 の実データと一致するかは未検証である**
（3 節 5）。表を作った時点で確かめる。噛み合わなければ入力を作らずに落ちる。

**2. 文字を見せるかどうか**

ハエは字を読めないが、`BTC_JPY` と気配の数字は画素として入力される。
価格が変われば模様も変わるので、**意味のない変動が混ざっている可能性がある。**

Stonkfly も同じもの（ペア名と気配）を見せているので、いまは合わせてある。
測るなら Phase 4 で「文字なしの腕」と比べる（7 節）。

### Phase 3：重み固定のシミュレーションで方向を返す

コネクトームを LIF で動かし、接近側・回避側のニューロン群の発火数から
方向を返す出どころ（ハエ版）を作る。**学習は入れない。**

#### 決めたこと

| 項目             | 決定                                                                    |
| ---------------- | ----------------------------------------------------------------------- |
| ニューロンモデル | LIF。パラメータは Stonkfly / Shiu ら (2024) を初期値にする              |
| 神経時間         | 観測ごとに 0.5 秒（`agent.yaml`）                                       |
| 読み出し         | 接近の指標 − 回避の指標。正の閾値超で `APPROACH`、負の閾値未満で `AVOID`、間は `NONE` |
| 対象ニューロン   | 接近側・回避側それぞれ**未決**（候補は設計メモ 3.3 節）。`agent.yaml` に置く |
| 全脳か部分か     | 性能を測ってから決める。定期実行の環境で回らないなら部分切り出しを検討する |
| 失敗時           | シミュレーション失敗・入力欠損は出どころの失敗として扱い、`direction.on_failure` に従う |

#### 完了の目安

- ハエ版が Phase 1 の契約を満たし、`direction.source` で切り替えられる
- 判断ログに、接近側・回避側の発火数と方向の根拠が残る
- 1回の観測が `runtime.max_runtime_sec` に収まる（収まらなければ部分切り出しへ）

### Phase 4：評価（固定版）

設計メモ 4 章の評価 1・3・4 を回し、固定版の挙動と偏りを記録する。

| 評価 | 内容                                                     | 本 Phase |
| ---- | -------------------------------------------------------- | -------- |
| 1    | 重み固定版と学習版の並走                                 | 固定版の記録だけ取る（学習版は Phase 6） |
| 3    | 檻だけ（常時 `APPROACH`）との比較                        | 回す     |
| 4    | 同じ頻度でランダムに `APPROACH` / `AVOID` を出す対照群との比較 | **回す（最重要）** |

#### 決めたこと

- 評価は同じ期間の15分足で行い、成績の物差しはナンピノニクスの `performance.py` 相当
  （総資産の推移・ドローダウン・Buy&Hold 比較）を流用する
- ランダムの `APPROACH` / `AVOID` / `NONE` の頻度は、固定版が同じ期間に出した頻度に合わせる
- 固定版の偏り（いつも `APPROACH`、いつも `AVOID`）を先に測る。
  偏りがあれば、それは市場の洞察ではなくネットワークの癖として記録する
- 儲かったかではなく、**ランダムと区別できるか**を見る

#### 腕を並べる（replay はしない）

評価 3・4 は**腕（arm）どうしの比較**で行う。腕とは、方向の出どころだけが違う設定のこと。

過去の足を使った replay（再実行）は**しない。** `bitbank paper` の約定判定を自前で真似ると、
真似が本物とずれた分だけ評価が嘘になる。代わりに、腕ごとにペーパー口座の状態ファイルと
判断ログを分け、同じ生の相場を見ながら同時に走らせる。

```bash
python3 scripts/make_arm.py cage-only --source always_approach
python3 scripts/make_arm.py fly       --source fly
# ハエの頻度が出たら、その値を写してランダム対照群を作る
python3 scripts/make_arm.py random --source random --seed 20260915 \
  --weights '{"APPROACH":0.5,"AVOID":0.2,"NONE":0.3}'

python3 -m dopabae.run --config arms/fly.yaml    # 腕ごとに15分おき
python3 -m dopabae.evaluate --config arms/fly.yaml --config arms/random.yaml
```

`arms/` は生成物で Git 管理外。正本は `agent.yaml` であり、腕は方向の出どころと
置き場所だけを差し替える。檻とリスクの値は正本と同じにする。違えば、腕の差が
方向の差でなくなる。

#### 測るもの

| 何を                 | なぜ                                                         |
| -------------------- | ------------------------------------------------------------ |
| 方向の頻度と偏り     | いつも同じ方向なら、相場の洞察ではなくネットワークの癖         |
| 檻の扱いの内訳       | 方向が壁で消えているなら、成績の差は方向の差ではない           |
| 方向のあとの値動き   | `APPROACH` のあとと `AVOID` のあとで違わなければ、方向に情報がない |
| 成績                 | 総資産・最大ドローダウン・Buy&Hold 比較                        |

方向のあとの値動きの差には**並べ替え検定**を当てる。分布を仮定せず、種を固定するので
同じ入力なら同じ p 値が出る。標本が各 30 件に満たないうちは `sample_warning` を添え、
その差は偶然と区別できないと明記する。

ランダム対照群の頻度は、比べる相手が同じ期間に出した頻度に合わせる
（`matched_random_weights` がその値を出す）。頻度が違う対照群と比べると、
方向の**中身**の差ではなく頻度の差を見てしまう。

**`evaluate.py` は結論（`verdict`）を書かない。** 数字を出すだけで、
「学んだ」と言えるかは人間が判断する。

#### 完了の目安

- 評価 3・4 の結果と、固定版の方向の記録が `docs/` または `records/` に残る
- ハエの方向がランダムと区別できるかの所見が書ける（区別できなくてもよい。それも結果）

#### 状態（2026-09-15）

土台（`performance.py` / `evaluate.py` / `scripts/make_arm.py`）を実装した。
合成データで、信号のある腕（p=0.0001）と同じ頻度のランダム（p=0.34）を
見分けられることを確かめてある。**評価そのものはまだ回していない。**
比べる相手のハエ版が Phase 3 で出てから回す。

### Phase 5：可塑性と報酬

Stonkfly の設計を出発点に、決済が確定した回だけ報酬を出す。

#### 決めたこと

| 項目             | Stonkfly                                     | 本リポジトリ                                       |
| ---------------- | -------------------------------------------- | -------------------------------------------------- |
| 報酬のタイミング | 観測ごと（含み損益込み）                     | **決済が確定した回だけ**                           |
| 報酬の閾値       | ±0.01 USDC                                   | 手数料より十分大きい値（`agent.yaml`）             |
| 報酬の対象       | PAM11（報酬 15 細胞）/ PPL101（嫌悪 2 細胞） | 同じ細胞群から始める                               |
| 可塑性の場所     | KC→MBON07 / MBON11                           | 同じ                                               |
| 記憶の減衰       | 1,800 秒                                     | 運用スケール（日〜週）に伸ばす。**未検証の拡張**   |
| 学習ルール       | anti-Hebbian（抑圧のみ）                     | まずは同じ。「何もしない」に寄るかは観察対象       |

- 報酬の判定はペーパートレードの確定損益から取る。損益が取得できなかった回は
  報酬を出さず `null` を記録する
- 学習で変わるのはシナプス重みだけ。閾値・対象ニューロン・報酬の閾値は
  `agent.yaml` に置き、ハエが書き換えない（`CLAUDE.md`）
- 記憶の減衰を伸ばしたときに抑圧のみのルールが破綻しないかは、本 Phase で観察する

#### 完了の目安

- 判断ログに、その回に与えたドーパミン刺激（なし / 報酬 / 嫌悪）と根拠の損益が残る
- シナプス重みのスナップショットが一定間隔で保存される（Git 管理外）
- 重みをリセットすれば固定版と同じ方向に戻る

### Phase 6：評価（学習版）

設計メモ 4 章の評価 1〜4 をすべて回す。

| 評価 | 内容                                                     |
| ---- | -------------------------------------------------------- |
| 1    | 重み固定版と学習版の並走（同じ期間の15分足で方向の違いを見る） |
| 2    | 学習に使っていない期間での同じ比較                       |
| 3    | 檻だけとの比較                                           |
| 4    | ランダムとの比較                                         |

#### 完了の目安

- 評価 1〜4 の結果が記録される
- 「学んだ」と言えるかの所見が書ける。言えなくてもよい。それも結果

## 6. 定期運用と記録

15分ごとの定期実行は実装済み（`scripts/run_arms.py` と `scripts/launchd/` / `scripts/systemd/`）。
腕は**並行して**回す。順番に回すと、遅い腕のぶんだけ後ろの腕がずれた相場を見るため。

同じ腕の二重起動は `dopabae/lock.py` が `flock` で防ぐ。前の回がまだ走っていれば
その腕はこの回を飛ばす。ハエのシミュレーション（Phase 3）が入ると 1 回が長くなるので、
この守りが要る。腕ごとに上限（既定 600 秒）を置き、超えた腕は打ち切る。
打ち切られた腕はプロセスが死んでロックも外れ、次の回はやり直せる。

配線図が決まるまでは、**檻だけ**と**ランダム**の腕を回しておける。Phase 4 の評価 3・4 の
対照群が、ハエ版ができる前から貯まる。

判断ログには、ナンピノニクスの項目に加えて次を残す（設計メモ 3.6 節）。

- 見せた画像のハッシュと描画に使った足の範囲
- 接近側・回避側のニューロン群の発火数と、方向の根拠
- ハエの答えと、檻がそれをどう扱ったか
- その回に与えたドーパミン刺激（なし / 報酬 / 嫌悪）と根拠になった損益
- シナプス重みのスナップショット（一定間隔）

ハエの反応は観測値なので、そのまま書く。解釈は記録の言語化に閉じ込め、
判断ログには書かない。

## 7. 決めていないこと

設計メモ 6 章のとおり。本書では決めない。

- キャラクター設定（名前はドパバエに決まった。`character-design.yaml` は未着手）
- 配線図のデータソースとライセンス（3 節 4・5）
- 接近側・回避側の読み出しに使うニューロン群（3 節 6）
- 記憶の減衰を伸ばしたときに、抑圧のみの学習ルールが破綻しないか
- 全脳を回す計算資源をどこに置くか
- 信用取引（売りから入る `AVOID`）。`bitbank paper` が現物のみのため当面扱わない
