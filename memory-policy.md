# 記憶方式 — ドパバエ

> 保存先・保持期間の正は `agent.yaml` の `memory` セクションです。

## なぜ記憶を分けるのか

15分ごとに起動するエージェントは、毎回まっさらな状態から始まります。
建玉・クールダウン・当日の約定回数・平均取得単価は、すべて `bitbank paper` の実測
（`trade-history` と `pnl`）から毎回導出し、記憶しません。

ドパバエが「覚える」ものは 2 種類だけです。

1. **判断ログ**：何を見て、ハエが何と答え、檻がどう扱ったか（観測の記録）
2. **シナプス重み**：報酬学習で変わる唯一の場所（Phase 5 以降）

## 記憶の構造

| 層       | ファイル                                | 内容                           | 保持   |
| -------- | --------------------------------------- | ------------------------------ | ------ |
| 現在     | `bitbank paper` の実測                  | 今の状態そのもの               | 取引所側 |
| 短期     | `var/memory/decisions/{date}.jsonl`     | 15分ごとの判断1行              | 90日   |
| 中期     | `var/memory/daily/{date}.md`            | 1日の要約（未実装）            | 無期限 |
| 長期     | `var/memory/lessons.md`                 | 建玉1回ぶんの学び（未実装）    | 無期限 |
| 重み     | `var/memory/weights/{date}.npz`         | シナプス重みのスナップショット（Phase 5） | 無期限 |
| 画像     | `var/memory/vision/{date}/{run_id}.png` | ハエに見せた画像。人間が確認するため     | 無期限 |

`var/` は Git 管理外です。

## 現在：`bitbank paper` の実測

状態は毎回 CLI から導出します。クラッシュ・二重起動・人間の手動介入があっても壊れないためです。

| 値                  | 導出元                                        |
| ------------------- | --------------------------------------------- |
| 建玉・平均取得単価  | `paper pnl`（`avgCost` は買い手数料込み）     |
| 未約定注文          | `paper active-orders`                         |
| クールダウン        | 直近 buy 約定の `filledAt` ＋ 30 分            |
| 当日の約定回数      | `paper trade-history` の当日ぶん              |
| 総資産              | `paper assets` の残高 × 現在価格               |

`var/status.yaml` はスナップショットであり、状態の正ではありません。

## 短期：判断ログ

1周ごとに 1 行。HOLD でも必ず残します。ナンピノニクスの項目に加えて次を持ちます。

| キー         | 内容                                                              |
| ------------ | ----------------------------------------------------------------- |
| `account`    | 総資産と現金の**観測値**（`paper assets` 由来）。評価はここだけを読み、現金と建玉から復元しない。観測できなかった回は `null` |
| `vision`     | ハエに見せた画像。`sha256` / 大きさ / 足の種類と本数 / 足の範囲（ms）/ 描いたもの。描けなかった回は `null` |
| `direction`  | ハエの答え。`source` / `value` / `reason` / `error` / `seed`       |
| `cage`       | 檻がそれをどう扱ったか。`bought` / `sold` / `wall` / `cooldown` / `avoid_flat` / `still` / `no_direction` / `exit` / `recover` / `guard` / `dust` |
| `bid` / `ask` | 指値を置く根拠になった気配                                        |
| `config.fingerprint` | 檻・方向・ハエ（画像を含む）の設定の指紋。設定を変えた前後の回を混ぜないため |

Phase 3 以降で発火数、
Phase 5 以降でドーパミン刺激と根拠の損益を足します（`docs/DESIGN_MEMO.md` 3.6 節）。

## 書き込みのルール

- 観測していない値を書かない。取得できなかった値は `null`
- 価格は必ず CLI 応答由来。`sources` に出典コマンドと取得時刻を残す
- ハエの答えは観測値としてそのまま書く。解釈は記録の言語化（未実装）に閉じ込め、判断ログには書かない
- API キー・シークレット・プロファイル名は書かない（`memory.redact`）
- 数字はハエの言葉に混ぜない（`personality.md`「数字の扱い」）

## 評価が読むもの

評価（`dopabae/evaluate.py`）は判断ログだけを読みます。取引所には触れません。

- 総資産は `account.equity_jpy`（観測値）。観測できなかった回は飛ばし、前の値で埋めません
- 方向の頻度・偏りは `direction.value`、檻の扱いは `cage`
- 方向のあとの値動きは `price`。観測が欠けた区間は飛ばします
- 設定を変えた前後の回が混ざっていないかは `config.fingerprint` で見ます

腕（方向の出どころだけが違う設定）ごとに判断ログを分けます（`scripts/make_arm.py`）。
分けないと、同じログを奪い合って比較になりません。

## 忘れかた

判断ログは 90 日で消します。シナプス重みは消しません。
重みをリセットすれば固定版に戻ることを、Phase 5 の完了条件にしています。
