# 戦略 — ドパバエ

> 数値パラメータの正は `agent.yaml` です。本書は「なぜその設計なのか」を説明します。
> 売買の**方向**はハエ脳が決めます。本書が説明するのは、方向を受け取った
> 決定的コード（檻。`dopabae/cage.py`）がどう動くかです。

## 前提

- 対象は `btc_jpy` の**現物のみ**。売りから入る建玉は作らない
- 15分ごとに1回、方向（`APPROACH` / `AVOID` / `NONE`）を受け取る
- ハエは数量・価格・上限・諦めを知らない。すべて檻が `agent.yaml` の値で決める
- 判断できないときは HOLD

## 方向と檻の対応

| 方向       | 建玉なし                             | 建玉あり                                         |
| ---------- | ------------------------------------ | ------------------------------------------------ |
| `APPROACH` | 固定額の買い指値を**買い気配**に置く | 上限まで買い増す。上限なら持ち続ける（壁）       |
| `AVOID`    | 何もしない（現物のみ。記録には残す） | 全量の売り指値を**売り気配**に置く               |
| `NONE`     | 何もしない                           | 持ち続ける                                       |
| 得られない | 何もしない（HOLD）                   | 何もしない（HOLD）                               |

## 板の扱い

檻は板の状態を覚えません。**前回置いた指値は、この回までに約定しなければ取り消し**、
いまの方向から組み直します。買い指値も売り指値も同じです。

- `APPROACH` が続けば、気配に追従した買い指値が毎回置き直される
- `AVOID` のあと `NONE` になれば、売り指値は取り消され、建玉はそのまま残る
- 取消が「すでに約定していた」で返ったら、その回は新しい注文を出さない

## 買い（APPROACH）

1回の買い金額は固定（`strategy.entry.budget_jpy_per_order`）。ただし次のうち最も
厳しいものに切り詰める。

| 制約                       | 出典                             |
| -------------------------- | -------------------------------- |
| 1回の発注金額の上限         | `risk.per_order_max_jpy`         |
| 取得原価合計の上限          | `risk.max_position_ratio`        |
| 常に残す現金                | `risk.min_cash_reserve_ratio`    |

さらにペース制御がかかる。

- 約定後のクールダウン（`strategy.entry.cooldown_hours_after_fill`）。ラウンドをまたいでも効く
- 1日の約定回数の上限（`strategy.entry.max_fills_per_day`）
- 冬眠中（`risk.drawdown.halt_new_buys_pct`）は新規買いを出さない

止められた回は判断ログの `cage` に理由（`wall` / `cooldown`）が残る。
personality.md では「壁」「なんで壁」になる。

## 売り（AVOID）

建玉の全量（`strategy.exit.sell_ratio`）を売り気配に指値で置く。
建玉がなければ何もしないが、`AVOID` と答えたことは記録に残す（評価で `NONE` と区別する）。

## 檻の諦め（方向によらない）

方向より先に効く。ハエがどう答えても変わらない。

| 条件                                        | 動作                                   |
| ------------------------------------------- | -------------------------------------- |
| 総資産が `forced_exit_pct` 以下              | 全建玉を成行で手仕舞い、停止（HALTED） |
| 建玉の保有が `time_stop_days` を超えた       | 全建玉を成行で手仕舞い                 |
| 前回の約定判定から `stale_tick_hours` 超     | 未約定の指値をすべて取り消し、この回は何もしない |

## 状態

| 状態              | 意味                                       |
| ----------------- | ------------------------------------------ |
| `NOT_INITIALIZED` | ペーパー口座が未初期化                     |
| `IDLE`            | 建玉なし                                   |
| `HOLDING`         | 建玉あり                                   |
| `HIBERNATING`     | 新規買い停止（ドローダウン）。AVOID の売りは出せる |
| `HALTED`          | 強制手仕舞い後の停止                       |

## 15分ごとの処理

```text
1. status / circuit-break でガード          → 異常なら HOLD
2. ticker で気配と鮮度を観測                 → 古ければ HOLD
3. paper tick で約定を解決（--pair は付けない）
4. assets / pnl / active-orders / trade-history から状態を導出
5. 方向を得る（agent.yaml の direction.source）→ 得られなければ HOLD
6. 檻が取消と発注を組み立てる
7. 取消 → 発注の順に実行
8. 判断ログを追記し、var/status.yaml を書き出す
```

実行は `python3 -m dopabae.run` の1コマンド。`--dry-run` で発注せずに組み立てだけ見られる。
