# リスク制約 — ドパバエ

> 数値パラメータの正は `agent.yaml` です。本書は「なぜその制約なのか」を説明します。
> `personality.md` と矛盾する場合は本書が勝ちます（`CLAUDE.md`）。

## 原則

ハエは歯止めを持ちません。毎回近づき続けることも、毎回逃げ続けることもあります。
本書の制約はすべて檻（`dopabae/cage.py`）が持ち、**ハエは値を見ることも変えることもできません。**

ハエがどう答えても、最悪ケースは本書の値で決まります。

## 資金の制約

| 項目                     | 上限            | 根拠                                           |
| ------------------------ | --------------- | ---------------------------------------------- |
| 建玉の取得原価合計       | 初期資金の 80%  | ナンピノニクスと同じ値から始める               |
| 常時維持する現金         | 初期資金の 10%  | 何があっても手をつけない緩衝                   |
| 1回の発注金額            | 210,000 JPY     | 同上（1回の買いは 100,000 JPY なので実質効かない） |

## ペース制御

- 約定後 30 分は次の買いを出さない。**ラウンドをまたいでも効く**（売り切った直後に買い直さない）
- 1日の約定は 16 回まで
- 未約定の買い指値は同時に 1 本。前回の指値はこの回までに約定しなければ取り消す

毎回 `APPROACH` でも、上限 80% に達するまで最速で 8 回（4 時間）。
資金を守るのはペース制御ではなく、資金の上限とドローダウンの段階制御である。

## ドローダウンによる段階制御

| 総資産の初期比 | 動作                                                   |
| -------------- | ------------------------------------------------------ |
| -15% 以下      | 冬眠。新規買いを停止し、板の買い指値を取り消す。`AVOID` の売りは出せる |
| -25% 以下      | 全建玉を成行で手仕舞い、停止（HALTED）                 |

### この設計で許容する最大損失

初期資金の 25% ＋ 成行手仕舞いのスリッページ。
ペーパートレードなので実損はないが、この線を超える設計変更は人間が判断する。

## 現物のみ

売りから入る建玉（信用・レバレッジ）は作らない。`bitbank paper` も現物しか扱えない。
建玉なしの `AVOID` は何もしない。負の建玉を観測したら異常として HOLD する。

## 市場状態のガード

- 取引所の状態が `NORMAL` / `BUSY` / `VERY_BUSY` 以外なら判断しない
- サーキットブレイク中は判断しない
- ペアが取引可能でなければ判断しない

## データ品質のガード

- 価格データが 5 分より古ければ判断しない
- 気配が取れない、または買い気配が売り気配より高ければ判断しない
- 建玉と残高が 1 単位を超えて食い違えば判断せず、人間の確認を求める
- 方向を得られなければ判断しない（`direction.on_failure: hold`）

## 発注と取消の競合

取消が「すでに約定していた」で返ったら、その回は新しい注文を出さない。
約定は取消より優先される。

## 二重起動

15分ごとの定期実行で前の回がまだ走っていると、**同じペーパー口座を 2 つの実行が
同時に触る。** `paper tick` が二重に走り、同じ判断で 2 回発注しうる。

`dopabae/lock.py` が `flock` で防ぐ。ロックを取れなかった回は**何もせずに終わる。**
プロセスが死ねば OS が自動で外すので、異常終了してもロックは残らない。

ロックは腕ごとに分ける（`runtime.lock_path`）。分けないと腕どうしが互いを止める。

## 運用を中断したあとの復帰

`paper tick` が遡れるのは直近 24 時間まで。前回の約定判定からそれを超えて止まっていたら、
板の指値をすべて取り消し、その回は何もしない。

## 実行してはならないこと

- `bitbank trade create-order` / `bitbank trade cancel-order`（実資金）
- `bitbank paper reset`（運用実績の破壊）
- Stonkfly の live モード

`dopabae/cli.py` はこれらを組み立てた時点で例外を出す。`.claude/settings.json` でも二重化している。

## 例外時の既定動作

例外・データ欠損・取引所異常・シミュレーション失敗・入力画像の欠損は、すべて HOLD。
発注しないことによる機会損失は許容し、誤発注は許容しない。

## 人間の介入が必要なとき

- 建玉と残高の不一致
- HALTED になった
- 観測や発注の失敗が続く
