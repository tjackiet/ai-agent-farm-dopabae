# Repository Instructions

このリポジトリでは、AIエージェントファームの2体目の試験エージェント
「ドパバエ」（Dopabae）を開発する。

本書には、**毎回必ず守る不変のルール**だけを書く。
開発工程・将来構想・設計の背景は書かない。

## 文書の役割

| 文書                          | 役割                         | 作業指示か                     |
| ----------------------------- | ---------------------------- | ------------------------------ |
| `CLAUDE.md`（本書）           | 不変のルール                 | はい                           |
| `docs/REPOSITORY_PLAN.md`     | 本リポジトリの担当範囲       | はい（実装範囲の唯一の正）     |
| `docs/IMPLEMENTATION_PLAN.md` | 実装順序（Phase）           | はい（実装順序の正）           |
| `docs/DESIGN_MEMO.md`         | この個体の設計メモ           | いいえ（設計の背景。作業指示ではない） |

1体目のナンピノニクス（`tjackiet/ai-agent-farm-nampinonychus`）は
**参照するだけで、変更しない。** そちらの文書に書かれた項目を、
本リポジトリの作業対象と解釈しない。

## 作業前に確認すること

1. 本リポジトリの担当範囲は `docs/REPOSITORY_PLAN.md` を参照する
2. 実装の順序は `docs/IMPLEMENTATION_PLAN.md` を参照する
3. 担当範囲を超える実装は、指示なしに行わない

## 取引の制約

- 実取引は行わない。**ペーパートレードのみ**を扱う。
- 市場データ取得と注文には `bitbank-lab-cli`（`bitbank` コマンド）を利用する。
  姉妹プロジェクトの `bitbank-lab-mcp` は、本リポジトリでは利用しない。
- 以下は理由を問わず、実行も生成もしない。
  - `bitbank trade create-order` — 実資金の発注
  - `bitbank trade cancel-order` — 実資金の取消
  - `bitbank paper reset` — 運用実績の破壊
- 参考にした外部プロジェクト Stonkfly の実取引モード（`--live`）と
  取引所 API キーの扱いは、**持ち込まない。**
- **判断できないときは HOLD。** 例外・データ欠損・取引所異常時は発注しない。
  シミュレーションの失敗・入力画像の欠損も同じく HOLD とする。
  発注しないことによる機会損失は許容し、誤発注は許容しない。

## ハエ脳の関与の制約

- ハエ脳が決めるのは**方向だけ**（`APPROACH` / `AVOID` / `NONE` の三値）。
  1回の数量・価格・建玉上限・現金比率・クールダウン・保有上限・ドローダウン停止は
  決定的コード（檻）が `agent.yaml` の値で決め、ハエは触れない。
  ハエがどう答えても、最悪ケースは `agent.yaml` のリスク値で決まる。
- 扱うのは**現物のみ**。売りから入る建玉（信用・レバレッジ）は作らない。
  `bitbank paper` も現物しか扱えない。
- **学習で変わるのはシナプス重みだけ。** 読み出しの閾値・対象ニューロン・
  神経時間・報酬の閾値は `agent.yaml` に置き、ハエが書き換えない。
- 報酬（ドーパミン刺激）は**決済が確定した回だけ**出す。含み損益では出さない。
- 読み出しは設計者が決めた読み替えである。「ハエの脳に売買を判断する細胞がある」
  という書き方を、文書にも記録にもしない。

## 値と設定の扱い

- 数値パラメータの正は `agent.yaml`。`.md` と食い違う場合は `agent.yaml` を優先する。
- `personality.md` と `risk-policy.md` が矛盾する場合は `risk-policy.md` を優先する。
- 戦略・リスク制約の値をエージェント自身が書き換えない。変更は人間が判断する。

## 記録の制約

- 観測していない値を書かない。取得できなかった値は `null` のままにする。
  損益が取得できなかった回は報酬を出さず、`null` を記録する。
- 価格は必ず CLI 応答由来とし、数値には出典コマンドと取得時刻を添える。
- ハエの反応（発火数など）は観測値としてそのまま書く。解釈は記録の言語化に閉じ込め、
  判断ログには書かない。
- API キー・シークレット・プロファイル名は、ログにも記憶にも残さない。
